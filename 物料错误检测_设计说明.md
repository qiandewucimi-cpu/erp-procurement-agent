# 物料错误检测工具 · 设计说明

> 目标：把一线业务中"物料问题日堆积几百条、人工翻找错误"这个真实痛点，做成 Agent 的第二个业务工具，让 demo 从"单流程"变成"有真实痛点支撑的方案"。

## 一、设计目标

录单员上传 BOM 后，Agent 自动逐行校验物料，把错误**分级、分类、聚合**成一份可读报告，必要时**推送给录单员**。核心理念与现有 5 个工具一致：**检测和分级全由确定性代码完成，模型只负责理解意图和决定是否推送，不参与错误判断**。

## 二、新增/改动一览

| 文件 | 改动 | 说明 |
|---|---|---|
| `erp_agent/validator.py` | 新增 | `MaterialValidator`（单行校验）+ `MaterialAuditor`（批量审计+报告） |
| `erp_agent/tools.py` | 新增工具 | `detect_material_errors`（schema + 确定性实现） |
| `erp_agent/repository.py` | 新增 | `error_reports` 表 + `save_error_report()` + `error_reports()` |
| `tests/test_material_errors.py` | 新增 | 14 个测试（校验器 8 + 审计/工具 6） |
| `knowledge/物料错误检测规则.md` | 新增 | 规则库文档，供 RAG 检索 |

## 三、工具 schema（模型看到的接口）

```
detect_material_errors
  filename    : BOM 文件名，如「异常示例_BOM.xlsx」
  min_level   : "warning"(默认，全报) | "blocking"(只报阻断)   [可选]
  push        : true 时把报告写入推送队列，默认 false 只检测   [可选]
```

## 四、7 种错误分类（分级）

| 类型 key | 中文 | 级别 | 触发条件 |
|---|---|---|---|
| `empty_code` | 编码为空 | 阻断 | 物料编码缺失 |
| `invalid_qty` | 数量非法 | 阻断 | 数量缺失/非数字/≤0 |
| `not_filed` | 未建档 | 阻断 | 编码在 ERP 档案不存在 |
| `name_mismatch` | 名称不一致 | 警告 | BOM 名称 ≠ 档案名称，以档案为准 |
| `missing_price` | 单价缺失 | 阻断 | 采购单价缺失或非正数 |
| `missing_packaging` | 包装费缺失 | 警告 | 包装费缺失 |
| `missing_supplier` | 供应商缺失 | 阻断 | 供应商编码/名称缺失 |

## 五、报告结构（真实输出示例）

对 `异常示例_BOM.xlsx`（3 行：名称不一致 / 未建档 / 数量 0）检测：

```json
{
  "ok": true,
  "report_id": "ERR-XXXXXXXXXX",
  "pushed": true,
  "summary": {
    "total_rows": 3, "error_count": 3, "blocking": 2, "warning": 1,
    "by_type": {
      "not_filed":      {"label": "未建档",   "level": "blocking", "count": 1},
      "invalid_qty":    {"label": "数量非法", "level": "blocking", "count": 1},
      "name_mismatch":  {"label": "名称不一致","level": "warning",  "count": 1}
    }
  },
  "errors": [
    {"row": 1, "material_code": "MAT-FAB-001", "type": "name_mismatch", "level": "warning", "message": "BOM 名称“涤纶面料（旧名称）”与档案“再生涤纶面料”不一致，以档案为准"},
    {"row": 2, "material_code": "MAT-UNKNOWN-999", "type": "not_filed", "level": "blocking", "message": "物料未建档，需人工建档或修正编码"},
    {"row": 3, "material_code": "MAT-ZIP-002", "type": "invalid_qty", "level": "blocking", "message": "数量必须是大于 0 的数字"}
  ],
  "push_text": "【物料错误报告】共 3 行，发现 3 个问题（阻断 2 / 警告 1）\n- [警告] 第1行 MAT-FAB-001：BOM 名称“涤纶面料（旧名称）”与档案“再生涤纶面料”不一致，以档案为准\n- [阻断] 第2行 MAT-UNKNOWN-999：物料未建档，需人工建档或修正编码\n- [阻断] 第3行 MAT-ZIP-002：数量必须是大于 0 的数字"
}
```

`push_text` 就是能直接发飞书/邮件的纯文本。

## 六、推送落库设计（outbox 模式）

`error_reports` 表充当**消息队列的 outbox**：`push=true` 时报告写入 `PENDING_PUSH` 状态，同时写一条 `ERROR_REPORT_CREATED` 审计。真实落地时，用一个 worker 轮询这张表、调飞书/邮件 webhook 推送、再把状态改 `PUSHED`——**检测和推送解耦，检测失败不影响业务，推送失败可重试**。

## 七、验证结果

```
Ran 48 tests in 19.049s
OK
```

原 34 个测试零回归，新增 14 个（校验器 8 + 审计聚合/工具/落库 6）全部通过。

## 八、与现有工具的关系 & 后续

- **复用**：`MaterialValidator` 把校验规则独立出来，`create_purchase_order` 未来可切到同一套校验器，消除现有的重复校验逻辑。
- **真实落地三件套**：① 定时任务（每天/每小时扫新 BOM）；② 飞书/邮件 webhook 替换 outbox；③ 错误报告加"已处理/忽略"状态形成闭环。
- **价值定位**：从"AI 生成采购单"升级成"AI 生成采购单 + 主动发现并推送物料错误"，第二个能力正是一线业务中的真实痛点。
