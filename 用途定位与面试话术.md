# 用途定位与面试话术

> 解决「这个 demo 到底干嘛的、有什么价值」说不清的问题。
> 一句话原则：**先讲业务痛点和价值，再讲技术**。面试官/领导在乎的是"你解决了什么"，不是"你用了什么"。

---

## 一、一句话定位（30 秒电梯陈述，先背这个）

> 我做了个**外贸 ERP 的安全型 AI 操作助手**：录单员不用再在 BOM、价格表、包装费表、ERP 页面之间来回切、手工搬数据，只要用自然语言说一句「根据这份 BOM 生成采购 PO」，Agent 就自动完成「查规则 → 解析 BOM → 查物料价格 → 算金额 → 校验 → 生成草稿」，**但只有人明确确认后才会真正写入 ERP**。核心卖点不是"AI 会干活"，而是"AI 干活但不会乱写"。

## 二、它到底解决什么真实问题（结合你的一线经历）

录单员每天的真实痛苦，你亲历过：

- 一份 BOM 要对着 **BOM、价格资料、包装费资料、原始 PO、ERP 页面** 五处来回切，手工搬运字段；
- 手工搬运 → **漏填、错填、重复等待**（这就是公司物料问题日堆积几百条的根源之一）；
- 字段规则记不清，得翻培训文档、问同事，新人上手慢。

Agent 把"跨表找数、取数、算数、校验"这些**机械活**自动化，人只保留"判断 + 确认"。

> 一句话版本：**它把录单员从"搬运工"变成"审核员"。**

## 三、它最值钱的地方（面试官真正关心的）

企业不敢让 AI 直接写 ERP，核心顾虑是 **AI 幻觉乱写数据**。这个 demo 的设计恰恰回答了这个顾虑：

| 环节 | 谁来做 |
|---|---|
| 理解意图、决定调哪个工具 | 大模型（可替换，失败自动回退规则） |
| 算金额、业务校验、写入权限 | **确定性代码**，模型碰不到 |

所以一句话戳中要害：**模型只做编排、不做决策，断网或幻觉都不会错误写入。** 这是"AI 能不能上生产"这个问题的工程答案，不是"我调了个 API"。

## 四、面试官三连问 + 应对口径

**Q1：这不就是个 RAG demo 吗？**
A：RAG（查规则）只是五个工具之一。核心是**工具调用循环 Agent**——模型自主决定调哪个工具、传什么参数、按什么顺序，这是 agent 和固定流水线的本质区别；且每个写操作都有安全边界兜底。

**Q2：数据都是假的，有什么意义？**
A：故意用合成数据 + 模拟 ERP，一是**合规**（不把公司生产数据带出、不用未授权系统），二是证明**可脱离公司环境独立复现**。真实落地时把模拟 ERP 换成 Adapter 接真实系统即可，安全边界和校验逻辑不变。

**Q3：为什么不让 AI 直接写库？**
A：企业写操作必须把确定性规则、权限、审计放在 LLM 之外。我设计了预览 → 口令确认 → 幂等 → 审计 → 回滚的完整链路，模型只负责编排，这是企业能接受 AI 的前提。

## 五、它和"普通 demo"的差异（背下这句）

> 别人做的是"AI 能生成采购单"，我做的是"**AI 生成采购单但全程可解释、可审计、可回滚、不会乱写**"。后者才是企业敢用的东西。

## 六、怎么让它更"入流"（升级路线，按性价比排序）

1. **扩一个真实业务场景**：把你公司最痛的「物料错误日堆积几百条、自动推送错误报告」做成第二个工具（检测→定位错误→推送），立刻从"单流程 demo"变"有真实痛点的方案"。
2. **加 Adapter 层**：抽一层 `ERPAdapter` 接口，模拟 ERP 只是其中一个实现，讲"未来换真实 API"就有底气了。
3. **RAG 升级 + 评测**：关键词检索 → BGE embedding + 向量库，补 Recall@K / 忠实度 / 拒答率指标。
4. **可观测**：结构化日志 + 每次工具调用的耗时/结果，面试时可说"我关注可维护性"。
5. **安全进阶**：加个极简的 RBAC / 审批状态机（哪怕 demo 级别）。

---

*背熟第一节的电梯陈述 + 第四节的三连问，面试时"用途不清"就解决了。*
