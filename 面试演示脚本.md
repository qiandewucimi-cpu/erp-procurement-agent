# 3 分钟面试演示脚本

## 0:00–0:30：问题与真实性

“我在外贸 ERP 培训和实操期间，亲自完成过从 BOM 到采购 PO 的完整流程。录单时需要跨 BOM、价格、包装费和系统档案取数，人工搬运慢且容易错，所以我做了这个安全型 ERP Agent 原型。为了合规，项目全部使用合成数据和模拟 ERP。”

## 0:30–1:20：正常流程（多轮对话 + 工具调用循环）

在聊天框输入：“帮我根据 `正常示例_BOM.xlsx` 生成采购 PO 草稿，先给我看金额”。边展示边说：

“这是工具调用循环 Agent，不是固定脚本。模型拿到任务后，自己决定调用哪些工具、传什么参数——它会调用 `create_purchase_order`，工具内部解析 BOM、查物料档案、算金额、做校验，返回一个待确认预览。你看到的总金额 ¥24164 是工具里的确定性代码算的，模型只负责编排，不碰数字。”

重点指出可展开的工具调用轨迹、采购明细的 `data_sources` 和 RAG 来源。

## 1:20–2:00：多轮与安全写入

先追一句“查一下 MAT-FAB-001 的价格”，展示模型只查询、不建单；再回到草稿，输入“确认提交”，展示模型调用 `confirm_commit` 写入。

“模型会记住上一轮的 action_id，只有用户明确输入‘确认提交’，写入工具才执行；同一个 action_id 幂等，重复确认不会重复建单。所有动作进审计日志。这是 agent 的自主性 + 确定性安全边界的结合。”

## 2:00–2:30：异常处理

切换异常 BOM：

“这里包含未建档物料、零数量和名称不一致。名称差异只警告，未建档和非法数量属于阻断错误，因此即使用户要求提交，后端也不会写入。”

## 2:30–3:00：架构与边界

“前端是 Streamlit 聊天界面，服务层是 FastAPI，模拟 ERP 用 SQLite。工具通过 Function Calling 暴露给模型，同时用 FastMCP 封装成标准 MCP 工具，任何 MCP 客户端都能接。生产化时我会把 ERP 接口封装为 Adapter，并补 SSO/RBAC、审批、密钥管理、监控和评测。作品没有连接公司系统，也没有使用真实客户数据。”

## 高频追问

### 为什么不让 LLM 直接生成和写入？

LLM 适合做意图理解、工具编排和非结构化信息处理，不适合决定金额、权限和最终写入。所以金额、校验、写入口令全部放在工具内部的确定性代码里，模型只负责“调哪个工具、传什么参数”，即使幻觉或断网也不会造成错误写入。

### 这算 Agent 还是固定脚本？

这是真正的工具调用循环 Agent：模型通过 Function Calling 自主决定调用哪些工具、传什么参数、顺序和次数，并支持多轮对话记忆（能记住上一轮的 action_id）。同时用确定性安全边界约束高风险写操作——自主编排和安全性并不矛盾。

### 为什么还要做一层 MCP？

MCP 让这 5 个业务工具变成标准化的可复用能力：不写死在某一个前端里，Claude Desktop、Cursor 或任何 MCP 客户端都能直接调用同一套工具。对 FDE 来说，这正对应“把企业系统能力以标准协议暴露给 AI”的核心工作。

### RAG 怎么评估？

准备一组真实但脱敏的问题，标注期望来源与答案，分别测检索 Recall@K、引用正确率、答案忠实度和拒答率；再调 chunk、top_k、embedding 与 reranker。

### 如果真实 ERP 没有 API？

优先推动自研系统提供受控 API；只读接口先行。直连数据库耦合和写错风险高，RPA 又容易受页面变化影响，只作为临时替代方案。

