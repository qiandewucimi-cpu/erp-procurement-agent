# 3 分钟面试演示脚本

## 0:00–0:30：问题与真实性

“我在外贸 ERP 培训和实操期间，亲自完成过从 BOM 到采购 PO 的完整流程。录单时需要跨 BOM、价格、包装费和系统档案取数，人工搬运慢且容易错，所以我做了这个安全型 ERP Agent 原型。为了合规，项目全部使用合成数据和模拟 ERP。”

## 0:30–1:20：正常流程

选择正常 BOM，点击“分析并生成草稿”。边展示边说：

“Agent 不是直接让模型编答案。它先检索采购规则，再解析 Excel，调用模拟 ERP 查询物料、供应商、价格和包装费，执行确定性校验，最后只生成变更预览。”

重点指出工具轨迹、采购明细的 `data_sources` 和 RAG 来源。

## 1:20–2:00：安全写入

故意先输入“确定”，展示接口拒绝；再输入“确认提交”。

“写操作必须显式确认，同一个 action_id 具备幂等性，重复点击不会重复创建采购单。所有动作进入审计日志。”

## 2:00–2:30：异常处理

切换异常 BOM：

“这里包含未建档物料、零数量和名称不一致。名称差异只警告，未建档和非法数量属于阻断错误，因此即使用户要求提交，后端也不会写入。”

## 2:30–3:00：架构与边界

“前端是 Streamlit，服务层是 FastAPI，模拟 ERP 使用 SQLite，规则库返回可追溯引用。当前使用受控工作流保证演示稳定；生产化时我会把 ERP 接口封装为 Adapter，并补 SSO/RBAC、审批、密钥管理、监控和评测。作品没有连接公司系统，也没有使用真实客户数据。”

## 高频追问

### 为什么不让 LLM 直接生成和写入？

LLM 适合做意图理解和非结构化信息处理，不适合决定金额、权限和最终写入。关键业务规则应由确定性代码校验，写入必须经过权限与人工确认。

### 这算 Agent 还是固定脚本？

这是面向高风险业务的受控工作流 Agent：它围绕目标调用知识检索、文件解析、ERP 查询、校验和写入工具，并保留过程轨迹。当前场景刻意限制自由度；后续可在安全边界内加入 LLM 规划器和更多工具。

### RAG 怎么评估？

准备一组真实但脱敏的问题，标注期望来源与答案，分别测检索 Recall@K、引用正确率、答案忠实度和拒答率；再调 chunk、top_k、embedding 与 reranker。

### 如果真实 ERP 没有 API？

优先推动自研系统提供受控 API；只读接口先行。直连数据库耦合和写错风险高，RPA 又容易受页面变化影响，只作为临时替代方案。

