# 05 验收标准与评测报告

## 第一阶段验收矩阵

| 能力 | 通过标准 | 当前证据 |
|---|---|---|
| 正常 BOM | 生成 3 行草稿，金额 ¥24164，写前不落正式单 | 单测与离线评测通过 |
| 异常 BOM | 未建档/非法数量阻断，名称差异告警 | 单测与评测通过 |
| 安全写入 | 无可信 action、错误口令、越权、自审均被阻断 | 权限、策略与模型对抗评测通过 |
| 状态与审计 | 审批、提交、回滚按状态机迁移且记录身份 | 状态机测试通过 |
| 幂等与并发 | 四路并发确认只生成一个 PO，调用方得到幂等结果 | 并发评测通过 |
| ERP 集成边界 | HTTP 鉴权、超时、重试、错误映射、幂等头可测试 | Adapter 单测通过 |
| 多入口 | REST、Web、MCP、飞书复用安全工具层 | MCP 8 工具冒烟；既有 UI/飞书验证记录 |
| 可观测性 | 返回 request_id，日志脱敏，指标可查询 | 可观测性测试通过 |

## 最近一次基线

- 单元测试：88/88 通过（S5，2026-09-13）。
- 确定性评测：25/25 通过。
- 最近一次含真实模型的全量评测：40/40 通过；报告见 `evals/REPORT.md`。
- 代码覆盖率：最近测量 87%。
- MCP：stdio 冒烟暴露 8 个工具。

真实模型结果可能因模型版本与网络漂移；CI 因此默认运行确定性层。任何简历或演示数字都应引用一次明确的运行记录，不把历史结果描述为永久保证。

## 复现命令

```powershell
.venv\Scripts\python.exe -m unittest discover -s tests -v
.venv\Scripts\python.exe evals\run_eval.py --offline
.venv\Scripts\python.exe smoke_mcp.py
```

配置支持 Function Calling 的模型后，去掉 `--offline` 运行全量评测。完整输出和各维度结果保存在 `evals/REPORT.md` 与 `evals/results.json`。
